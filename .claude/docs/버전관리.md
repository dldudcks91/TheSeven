# 버전관리.md - TheSeven 개발 이력

> **프로젝트**: TheSeven (FastAPI 4X 전략 게임 백엔드)
> **목적**: 포트폴리오
> **최종 수정**: 2026-03-13 (부하 테스트 프레임워크)

---

## 버전 이력 요약

| 버전 | 주요 기능 | 상태 |
|------|----------|------|
| v0.1 | 프레임워크 기반, 로그인/자원 시스템 | ✅ 완료 |
| v0.2 | 건물/연구 시스템 | ✅ 완료 |
| v0.3 | 유닛 시스템 | ✅ 완료 |
| v0.4 | 미션/아이템/상점/버프 시스템 | ✅ 완료 |
| v0.5 | 연맹 시스템 | ✅ 완료 |
| v0.6 | 전투 시스템 (맵/행군/전투) | ✅ 완료 |
| v0.6.1 | NPC 사냥 시스템 + 클라이언트 개선 | ✅ 완료 |
| v0.6.2 | 실시간 맵 멀티플레이어 동기화 + 버그 수정 | ✅ 완료 |
| v0.6.3 | March DB 제거 → Redis-only 전환 | ✅ 완료 |
| v0.6.4 | 성 일반전투 (multi-attacker + 무혈입성) | ✅ 완료 |
| infra | Claude Code 구조 재편 (rules/hooks/memory) | ✅ 완료 |
| v0.7 | C++ 전투 계산 포팅 | ⏳ 예정 |

---

## v0.1 — 프레임워크 기반 · 로그인 · 자원

### 아키텍처 확립

- **Redis-first Cache-Aside** 패턴 도입
  - 모든 읽기/쓰기는 Redis 경유
  - MySQL은 영속성 보장용 (SyncWorker가 주기적 동기화)
- **APIManager 패턴** 확립
  - `POST /api { user_no, api_code, data }` 단일 엔드포인트
  - `api_map = { code: (ManagerClass, "method") }` 라우팅 테이블
  - Game Manager 메서드는 파라미터 없이 `self.user_no`, `self.data` 사용
- **BackgroundWorker** 기반 구조
  - BaseWorker → SyncWorker (카테고리별), TaskWorker (완료 이벤트)
- **WebSocketManager**: 작업 완료 시 실시간 알림

### 신규 파일

| 파일 | 내용 |
|------|------|
| `main.py` | FastAPI 진입점, 의존성 주입, startup_event |
| `models.py` | SQLAlchemy ORM (StatNation, Resources 등) |
| `schemas.py` | Pydantic ApiRequest 스키마 |
| `database.py` | DB 연결 설정 (SessionLocal) |
| `services/system/APIManager.py` | api_code 라우팅 |
| `services/system/GameDataManager.py` | CSV 메타데이터 로드 (서버 시작 1회) |
| `services/system/WebsocketManager.py` | WebSocket 연결 관리 |
| `services/game/LoginManager.py` | 로그인 (asyncio.gather 병렬 로드) |
| `services/game/ResourceManager.py` | 자원 조회 |
| `services/redis_manager/ResourceRedisManager.py` | 자원 Redis (Lua 원자적 차감) |
| `services/db_manager/NationDBManager.py` | 국가 DB CRUD |
| `services/background_workers/ResourceSyncWorker.py` | 자원 Redis→MySQL (60초) |
| `templates/main.html` | 메인 탭 레이아웃 (iframe 기반) |
| `templates/resource.html` | 자원 현황 페이지 |

### API 코드

| 코드 | 기능 |
|------|------|
| 1002 | 게임 설정 데이터 (GameDataManager) |
| 1003 | 신규 유저 생성 (UserInitManager) |
| 1009 | 국가 정보 조회 |
| 1010 | 로그인 (전체 데이터 병렬 로드) |
| 1011 | 자원 정보 조회 |

### Redis 키 구조 (초기)

```
user_data:{user_no}:{category}   - Hash  (유저 데이터 캐시)
sync_pending:{category}          - Set   (Dirty flag)
completion_queue:{task_type}     - ZSet  (완료 타이머, score=timestamp)
nation_data:{user_no}            - Hash  (국가 데이터)
```

---

## v0.2 — 건물 · 연구 시스템

### 신규 기능

**건물 시스템**
- 건물 목록: 타운홀(201), 아카데미(301), 병영(401), 농장(501), 제재소(502), 채석장(503)
- 최대 레벨 10, 타운홀 레벨 연동 제한
- 건설/업그레이드 → 타이머 → TaskWorker 자동 완료
- 취소 → 자원 100% 환불
- `AVAILABLE_BUILDINGS` 상수로 건물 인덱스 목록 관리

**연구 시스템**
- 자원 계열(1xxx) / 병력 계열(2xxx) 트리 구조
- 동시 1개 연구 제한
- 연구 완료 시 BuffManager 통해 버프 적용

### 신규 파일

| 파일 | 내용 |
|------|------|
| `services/game/BuildingManager.py` | 건물 CRUD, 완료/취소 처리 |
| `services/game/ResearchManager.py` | 연구 시작/완료/취소 |
| `services/game/BuffManager.py` | 버프 조회/적용 |
| `services/redis_manager/BuildingRedisManager.py` | 건물 Redis |
| `services/redis_manager/ResearchRedisManager.py` | 연구 Redis |
| `services/db_manager/BuildingDBManager.py` | 건물 DB CRUD |
| `services/db_manager/ResearchDBManager.py` | 연구 DB CRUD |
| `services/background_workers/BuildingSyncWorker.py` | 건물 동기화 (10초) |
| `services/background_workers/ResearchSyncWorker.py` | 연구 동기화 (10초) |
| `meta_data/building_info.csv` | 건물 레벨별 비용/시간 |
| `meta_data/research_info.csv` | 연구 비용/시간/버프 |
| `meta_data/buff_info.csv` | 버프 효과 |
| `templates/building.html` | 건물 관리 페이지 |
| `templates/research.html` | 연구 트리 페이지 |

### API 코드

| 코드 | 기능 |
|------|------|
| 2001 | 건물 목록 조회 |
| 2002 | 건물 생성 |
| 2003 | 건물 업그레이드 시작 |
| 2004 | 건물 업그레이드 완료 |
| 2005 | 건물 업그레이드 취소 |
| 2006 | 건물 전체 즉시 완료 |
| 3001 | 연구 목록 조회 |
| 3002 | 연구 시작 |
| 3003 | 연구 완료 |
| 3004 | 연구 취소 |
| 1012 | 버프 정보 조회 |

---

## v0.3 — 유닛 시스템

### 신규 기능

- 보병(401~404) / 기병(411~414) / 궁병(421~424) — 각 4티어
- 티어 2 이상: 해당 연구 완료 후 해금
- 유닛 상태: ready / training / upgrading / field / injured / wounded / healing / death
- 훈련 타이머 → TaskWorker 자동 완료
- 유닛 업그레이드 (하위 티어 → 상위 티어 변환)
- `async` 메서드 전면 전환 (커밋 `3865185: python method to async`)

### 신규 파일

| 파일 | 내용 |
|------|------|
| `services/game/UnitManager.py` | 유닛 조회/훈련/업그레이드 |
| `services/redis_manager/UnitRedisManager.py` | 유닛 Redis |
| `services/db_manager/UnitDBManager.py` | 유닛 DB CRUD |
| `services/background_workers/UnitSyncWorker.py` | 유닛 동기화 (30초) |
| `meta_data/unit_info.csv` | 유닛별 스탯/티어/비용 |
| `templates/unit.html` | 유닛 훈련 페이지 |

### API 코드

| 코드 | 기능 |
|------|------|
| 4001 | 유닛 목록 조회 |
| 4002 | 유닛 훈련 |
| 4003 | 유닛 업그레이드 |

---

## v0.4 — 미션 · 아이템 · 상점 · 버프

### 신규 기능

**미션 시스템**
- 카테고리: building / unit / research / resource
- 완료 후 보상 수령 (별도 버튼 클릭 필요)
- 보상: 자원 / 아이템 / 버프

**아이템/상점 시스템**
- 아이템 카테고리: resource(자원 즉시 지급), speedup(시간 단축), buff(일시 버프)
- 상점: 가중치(weight) 기반 랜덤 진열, 새로고침 가능

### 신규 파일

| 파일 | 내용 |
|------|------|
| `services/game/MissionManager.py` | 미션 진행/보상 |
| `services/game/ItemManager.py` | 아이템 CRUD |
| `services/game/ShopManager.py` | 상점 진열/구매 |
| `services/db_manager/MissionDBManager.py` | 미션 DB |
| `services/db_manager/ItemDBManager.py` | 아이템 DB |
| `services/background_workers/MissionSyncWorker.py` | 미션 동기화 (120초) |
| `services/background_workers/ItemSyncWorker.py` | 아이템 동기화 |
| `meta_data/mission_info.csv` | 미션 조건/보상 |
| `meta_data/mission_reward.csv` | 미션 보상 세부 |
| `meta_data/item_info.csv` | 아이템 정보 |
| `meta_data/shop_info.csv` | 상점 가중치 |
| `templates/mission.html` | 미션 현황 페이지 |
| `templates/item.html` | 아이템 보관함 |
| `templates/shop.html` | 상점 페이지 |
| `templates/buff.html` | 버프 현황 페이지 |

### API 코드

| 코드 | 기능 |
|------|------|
| 5001 | 미션 목록 조회 |
| 5002 | 미션 보상 수령 |
| 6001 | 아이템 목록 조회 |
| 6002 | 아이템 획득 |
| 6003 | 아이템 사용 |
| 6011 | 상점 조회 |
| 6012 | 상점 새로고침 |
| 6013 | 상점 구매 |

---

## v0.5 — 연맹 시스템

> 참고 커밋: `9cd9421 alliance-add`

### 신규 기능

- 연맹 생성/해산/가입(자유/승인제)/탈퇴/추방
- 직책 체계: 맹주(1) / 부맹주(2) / 간부(3) / 일반(4) — 권한별 접근 제어
- 연맹 레벨: 멤버 자원 기부 → 연맹 EXP → 레벨업 → 최대 멤버 수 증가
- 연맹 연구: 기부 EXP 누적 → 간부 이상이 연구 시작 (자동 레벨업 없음)
  - 연구 목록: 공격력 강화(8001), 방어력 강화(8002), 자원 생산 강화(8003)
- 연맹 공지 작성/수정

### 신규 파일

| 파일 | 내용 |
|------|------|
| `services/game/AllianceManager.py` | 연맹 전체 비즈니스 로직 |
| `services/redis_manager/AllianceRedisManager.py` | 연맹 Redis |
| `services/db_manager/AllianceDBManager.py` | 연맹 DB CRUD |
| `services/background_workers/AllianceSyncWorker.py` | 연맹 동기화 (30초) |
| `meta_data/alliance_level.csv` | 연맹 레벨별 최대 멤버/필요 EXP |
| `meta_data/alliance_position.csv` | 직책별 권한 |
| `meta_data/alliance_research.csv` | 연맹 연구 레벨별 필요 EXP |
| `meta_data/alliance_donate.csv` | 자원별 기부 비율 |
| `templates/alliance.html` | 연맹 관리 페이지 (대폭 리뉴얼) |

### API 코드

| 코드 | 기능 |
|------|------|
| 7001 | 연맹 정보 조회 |
| 7002 | 연맹 생성 |
| 7003 | 연맹 해산 |
| 7004 | 연맹 가입 신청 |
| 7005 | 가입 신청 승인 |
| 7006 | 가입 신청 거절 |
| 7007 | 연맹 탈퇴 |
| 7008 | 멤버 추방 |
| 7009 | 직책 변경 |
| 7010 | 가입 방식 변경 (free/approval) |
| 7011 | 자원 기부 |
| 7012 | 연맹 공지 작성 |
| 7013 | 연맹 연구 목록 조회 |
| 7014 | 연맹 연구 추천 |
| 7015 | 연맹 연구 시작 |
| 7016 | 가입 신청 목록 조회 |
| 7017 | 가입 신청 취소 |

### DB 스키마 추가

```
Alliance        : alliance_no(PK), name, leader_no, level, exp, join_type, notice
AllianceMember  : alliance_no(PK), user_no(PK), position, donated_exp, donated_coin
AllianceApplication : alliance_no(PK), user_no(PK), applied_at
AllianceResearch    : alliance_no(PK), research_idx(PK), level, current_exp, is_active
```

---

## v0.6 — 전투 시스템 (맵 · 행군 · 전투)

> 구현일: 2026-03-01
> 설계 문서: [`COMBAT.md`](COMBAT.md)

### 설계 결정 사항

| 항목 | 결정 |
|------|------|
| 맵 | 100×100 자유 좌표 |
| 전투 방식 | arrive-then-fight (이동 중 전투 없음) |
| 라운드 | 1초/라운드, 최대 50라운드 |
| 약탈 | 공격자 승리 시 방어자 자원 20% |
| 행군 속도 | 최소 unit.speed × 10 (tiles/min) |
| 동시 행군 | 플레이어당 최대 3개 |
| 영웅 보정 | hero_lv × 5% 공격력 보정 |
| 전투 계산 | Python 1차 구현 → C++ pybind11 포팅 예정 |

### 신규 파일

| 파일 | 내용 |
|------|------|
| `services/game/MapManager.py` | 위치 조회/배정, 주변 유저 조회 |
| `services/game/MarchManager.py` | 행군 목록/생성/취소 |
| `services/game/BattleManager.py` | 전투 시작/틱/종료/조회, `calculate_round()` |
| `services/redis_manager/CombatRedisManager.py` | 전투 관련 Redis 전담 |
| `services/redis_manager/redis_types.py` (수정) | MARCH, MARCH_RETURN TaskType 추가 |
| `services/db_manager/MarchDBManager.py` | March DB CRUD |
| `services/db_manager/BattleDBManager.py` | Battle DB CRUD |
| `services/background_workers/BattleWorker.py` | 1초 주기 전투 틱 처리 |
| `meta_data/hero_info.csv` | 영웅 기본 스탯 |
| `meta_data/hero_skill.csv` | 영웅 스킬 (플레이스홀더) |
| `templates/battle.html` | 맵/행군/전투 보고서/실시간 로그 |

### 수정 파일

| 파일 | 변경 내용 |
|------|---------|
| `models.py` | StatNation에 `map_x`, `map_y` 추가; `March`, `Battle` 테이블 신규 |
| `services/db_manager/NationDBManager.py` | `_serialize_model()`에 map_x/map_y 포함 |
| `services/redis_manager/RedisManager.py` | `get_combat_manager()` 추가 |
| `services/db_manager/DBManager.py` | `get_march_manager()`, `get_battle_manager()` 추가 |
| `services/system/GameDataManager.py` | hero/hero_skill 로드 추가 |
| `services/system/APIManager.py` | 9001~9022 라우팅 추가 |
| `services/background_workers/TaskWorker.py` | 행군 도착/귀환 처리 추가 |
| `services/background_workers/BackgroundWorkerManager.py` | BattleWorker 등록 |
| `templates/main.html` | 전투 탭 추가, WebSocket battle 이벤트 처리 |
| `routers/pages.py` | `/battle.html` 라우트 추가 |

### API 코드

| 코드 | 기능 |
|------|------|
| 8001 | 영웅 목록 조회 (도감 + 보유 여부) |
| 8002 | 영웅 지급 (테스트용) |
| 9001 | 내 위치 조회 |
| 9002 | 맵 정보 (주변 유저/NPC 조회) |
| 9003 | NPC 목록 조회 |
| 9011 | 행군 목록 조회 |
| 9012 | 행군 생성 (출진) — target_type: user/npc |
| 9013 | 행군 취소 |
| 9021 | 전투 상태 조회 |
| 9022 | 전투 보고서 조회 |

### Redis 키 추가

```
map:positions                      - Hash  { user_no → "x,y" }
completion_queue:march             - ZSet  (score=도착시각)
completion_queue:march_return      - ZSet  (score=귀환시각)
march:metadata:{march_id}          - String (JSON)
user_data:{user_no}:march          - String (JSON, 행군 목록 캐시)
battle:{battle_id}                 - Hash  (전투 상태)
battle:active                      - Set   (진행 중인 battle_id 목록)
```

### DB 스키마 추가

```sql
-- StatNation 컬럼 추가
ALTER TABLE stat_nation ADD COLUMN map_x INT, ADD COLUMN map_y INT;

-- March 테이블 신규 (target_type, npc_id 포함)
CREATE TABLE march (
    march_id VARCHAR(36) PRIMARY KEY,  -- UUID
    user_no INT, target_user_no INT NULL, npc_id VARCHAR(36) NULL,
    target_type VARCHAR(10),           -- "user" | "npc"
    from_x INT, from_y INT, to_x INT, to_y INT,
    units TEXT,  -- JSON
    hero_idx INT NULL,
    march_speed FLOAT,
    departure_time DATETIME, arrival_time DATETIME, return_time DATETIME NULL,
    status VARCHAR(20),  -- marching/battling/returning/completed/cancelled
    battle_id VARCHAR(36) NULL
);

-- Battle 테이블 신규
CREATE TABLE battle (
    battle_id VARCHAR(36) PRIMARY KEY,  -- UUID
    march_id VARCHAR(36),
    attacker_user_no INT, defender_user_no INT NULL, npc_id VARCHAR(36) NULL,
    attacker_win BOOLEAN NULL,
    rounds INT NULL,
    attacker_losses TEXT NULL,  -- JSON
    defender_losses TEXT NULL,  -- JSON
    loot TEXT NULL,             -- JSON
    hero_exp INT NULL,
    created_at DATETIME
);

-- Hero 테이블 신규
CREATE TABLE hero (
    user_no INT,
    hero_idx INT,
    hero_lv INT DEFAULT 1,
    exp INT DEFAULT 0,
    PRIMARY KEY (user_no, hero_idx)
);
```

### WebSocket 이벤트 (신규)

| 이벤트 | 트리거 | 수신자 |
|--------|--------|--------|
| `battle_incoming` | 적 행군 도착 감지 | 방어자 |
| `battle_start` | 전투 시작 | 공격자, 방어자 |
| `battle_tick` | 라운드 진행 | 공격자, 방어자 |
| `battle_end` | 전투 종료 | 공격자, 방어자 |
| `march_return` | 병력 귀환 | 공격자 |

---

## v0.6.1 — NPC 사냥 시스템 · 영웅 EXP · 클라이언트 개선

> 구현일: 2026-03-02

### 신규 기능

**NPC 사냥 시스템**
- `npc_info.csv` — NPC 종류, 티어, EXP 보상, 주둔 병력, 리스폰 시간
- `NpcManager` — 맵 초기화 시 NPC 랜덤 배치, API 9003 목록 조회
- March `target_type="npc"` 지원 — `npc_id` 필드 추가
- NPC 처치 후 `completion_queue:npc_respawn` 등록 → TaskWorker가 리스폰 처리

**영웅 EXP 시스템**
- `HeroManager.add_hero_exp()` — EXP 누적 + 레벨업 처리
- NPC 처치 시 동행 영웅에게 EXP 지급 (`npc_info.csv`의 `exp_reward` 기준)
- `Hero` DB 테이블 구현 (hero_idx, hero_lv, exp)

**클라이언트 battle.html 개선**
- 맵 Canvas 280×280 → **600×600** 확대
- 행군 실시간 애니메이션 (requestAnimationFrame, 선형 보간)
- NPC 클릭 팝업 — 클릭한 NPC에 바로 출진 (유닛/영웅 드롭다운 선택)
- NPC 목록 카드, NPC 출진 카드 제거 → 팝업으로 통합
- UTC 시간 파싱 버그 수정 (`parseDT()` — 'Z' suffix 보장)

### 신규 파일

| 파일 | 내용 |
|------|------|
| `services/game/NpcManager.py` | NPC 초기화/조회 |
| `services/game/HeroManager.py` | 영웅 목록/지급/EXP 처리 |
| `services/db_manager/HeroDBManager.py` | Hero DB CRUD |
| `meta_data/npc_info.csv` | NPC 종류별 스탯/보상 |

### 수정 파일

| 파일 | 변경 내용 |
|------|---------|
| `services/game/MarchManager.py` | target_type="npc" 처리, npc_id 검증 |
| `services/game/BattleManager.py` | NPC 전투 종료 시 EXP 지급, npc alive=false |
| `services/redis_manager/CombatRedisManager.py` | map:npcs Hash 관리, npc_respawn 큐 |
| `services/db_manager/MarchDBManager.py` | target_type, npc_id 컬럼 |
| `services/system/APIManager.py` | 8001/8002/9003 라우팅 추가 |
| `services/system/GameDataManager.py` | npc CSV 로드 추가 |
| `services/background_workers/TaskWorker.py` | npc_respawn 큐 처리 추가 |
| `services/db_manager/DBManager.py` | `get_hero_manager()` 추가 |
| `templates/battle.html` | 맵 확대, 행군 애니메이션, NPC 팝업 |

### Redis 키 추가

```
map:npcs                              - Hash  { npc_id → JSON }
completion_queue:npc_respawn          - ZSet  (score=리스폰시각)
```

---

## v0.6.2 — 실시간 맵 멀티플레이어 동기화 · 버그 수정

> 구현일: 2026-03-03

### 신규 기능

**실시간 맵 행군 브로드캐스트**
- 행군 생성/취소/도착/귀환 시 모든 연결 클라이언트에게 WebSocket 브로드캐스트
- `globalMarches{}` — 클라이언트 사이드 전체 유저 행군 딕셔너리 (페이지 로드 시 API 9002 초기화)
- 이벤트: `map_march_start` / `map_march_update` / `map_march_complete`
- 내 행군 (주황) / 타 유저 행군 (보라) / 귀환 중 (파랑) / 전투 중 (빨강) 색상 구분

**영웅 중복 출전 방지**
- `march_create` — PvP / NPC / location 3개 분기 모두에 hero_idx 충돌 체크 추가
- 이미 출전 중인 영웅 재선택 시 `success: false, "해당 영웅은 이미 출전 중입니다"` 반환

### 버그 수정

**전투 종료 후 맵에 "전투 중" 상태 잔류 버그**
- `BattleWorker`: 전투 종료(`finished=True`) 시 `map_march_update {status: "returning"}` 브로드캐스트 추가
- 기존: `battle_end` 개인 알림만 → 타 클라이언트 맵에서 계속 "전투 중"으로 표시되는 버그

### 수정 파일

| 파일 | 변경 내용 |
|------|---------|
| `main.py` | `get_api_manager()` — ws_manager 의존성 주입 추가 |
| `services/system/APIManager.py` | `websocket_manager` 수신 + `service_instance`에 주입 |
| `services/db_manager/MarchDBManager.py` | `get_all_active_marches()` 신규 — 맵 초기 로드용 |
| `services/game/MapManager.py` | `map_info()` 응답에 `all_marches` 포함 |
| `services/game/MarchManager.py` | 행군 생성 시 WS 브로드캐스트 + 영웅 중복 출전 방지 |
| `services/background_workers/TaskWorker.py` | 도착/귀환 시 `map_march_update/complete` 브로드캐스트 |
| `services/background_workers/BattleWorker.py` | 전투 종료 시 `map_march_update {returning}` 브로드캐스트 |
| `templates/main.html` | `map_march_start/update/complete` 이벤트 battleFrame 포워딩 |
| `templates/battle.html` | `globalMarches` 관리, WS 이벤트 핸들러, 행군 색상 구분 범례 |

### WebSocket 이벤트 추가 (브로드캐스트)

| 이벤트 | 트리거 | 수신자 |
|--------|--------|--------|
| `map_march_start` | 행군 생성 | 전체 클라이언트 |
| `map_march_update` | 상태 변경 (battling/returning) | 전체 클라이언트 |
| `map_march_complete` | 행군 종료/취소/귀환 완료 | 전체 클라이언트 |

---

## [2026-03-08] 테스트 인프라 — Resource / Item / Shop 도메인 테스트 추가

### 작업 내용

- Resource(1011), Item(6001~6003), Shop(6011~6013) 도메인 테스트 파일 신규 작성
- 총 41개 테스트 추가 (기존 80 → **121 passed**)

### 신규 파일

| 파일 | 테스트 수 | 커버리지 |
|------|----------|---------|
| `tests/test_resource.py` | 5 | API 1011 (자원 조회, DB 미스 폴백, Redis 캐시 히트, 직접 변경 반영) |
| `tests/test_item.py` | 16 | API 6001/6002/6003 (목록 조회, 획득/누적, 사용/자원 효과/소진) |
| `tests/test_shop.py` | 20 | API 6011/6012/6013 (조회/생성, 새로고침, 구매/중복방지/슬롯검증) |

### 주요 발견 사항

- `CacheType.RESOURCES` (복수형) — 단수형 `RESOURCE`로 작성 시 AttributeError (수정 완료)
- `ShopManager.shop_buy()` — 빈 dict `{}` 전달 시 `"Missing data"` 반환 (slot 누락 메시지 아님)

---

## v0.6.3 — March DB 제거 → Redis-only 전환

> 구현일: 2026-03-08

### 변경 동기

March(행군)은 순수 임시 상태로, DB에 영속 기록이 불필요. 기존 March DB 테이블은 Redis 장애 시 복구용이었으나, completion_queue 자체가 Redis에 있어 실질적 복구 불가. Redis-only로 단순화.

### 변경 내용

**삭제 파일**
- `services/db_manager/march_db_manager.py` — 전체 삭제

**수정 파일**

| 파일 | 변경 내용 |
|------|---------|
| `services/redis_manager/combat_redis_manager.py` | `generate_march_id()` INCR, `update_march_metadata()`, `user:active_marches` Set 관리 추가. `set_march_metadata()` TTL 제거. `get_user_marches()`/`set_user_marches()`/`invalidate_user_marches()` 제거 |
| `services/game/MarchManager.py` | DB 호출 전부 Redis metadata로 전환. `_check_march_limits()`, `_validate_units()`, `_deduct_units()` 헬퍼 추출 |
| `services/game/BattleManager.py` | `march_dm.get_march()` → `combat_rm.get_march_metadata()`, `march_dm.update_march_status()` → `combat_rm.update_march_metadata()` |
| `services/background_workers/task_worker.py` | `_handle_march_returns()` DB 의존 제거, `_handle_march_arrivals()` location 분기 DB 제거 |
| `services/background_workers/battle_worker.py` | march DB 조회 → Redis metadata 조회 |
| `services/game/MapManager.py` | `map_info()` DB 행군 목록 → Redis 큐 기반 조회 |
| `services/db_manager/DBManager.py` | `get_march_manager()` 제거 |
| `services/db_manager/__init__.py` | `MarchDBManager` import/export 제거 |
| `models.py` | `March` 클래스 제거 |

### Redis 키 변경

| 키 | 변경 |
|----|------|
| `march:id:counter` | **신규** — INCR로 march_id 원자적 생성 |
| `march:metadata:{march_id}` | TTL 제거, status/battle_id/return_time 필드 추가 |
| `user:active_marches:{user_no}` | **신규** — 유저별 활성 march_id Set |
| `user_data:{user_no}:march` | **제거** — active_marches Set으로 대체 |

### 알려진 한계

- Redis 재시작 시 진행 중인 행군 유실 (AOF persistence 권장)
- march 완료/취소 외의 비정상 종료 시 metadata가 남을 수 있음 (주기적 cleanup 미구현)

---

## [2026-03-09] Buff 미해결 이슈 수정 + 테스트 추가

### 작업 내용

- Buff ISSUE #1 해결: `buff_total_info`(1013), `buff_total_by_type_info`(1014) APIManager 등록
- Buff ISSUE #2 해결: `base_redis_task_manager.py`의 `get_completed_tasks`에서 `sub_id` KeyError 수정
- `buff_total_by_type_info` 시그니처를 APIManager 패턴에 맞게 `self.data` 기반으로 변경
- `buff.html` 클라이언트 api_code 1111 → 1013 수정
- 1013/1014 API 테스트 6개 추가 → 전체 **127 passed**

### 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| `services/system/APIManager.py` | 1013, 1014 라우팅 등록 |
| `services/game/BuffManager.py` | `buff_total_by_type_info` 시그니처 변경 |
| `services/redis_manager/base_redis_task_manager.py` | `parsed.get('sub_id')` 수정 |
| `templates/buff.html` | api_code 1111 → 1013 |
| `tests/test_buff.py` | 1013/1014 테스트 6개 추가 |
| `.claude/docs/API.md` | 1013, 1014 명세 추가 |

---

## [2026-03-09] 연맹 버프 시스템 구현

### 작업 내용

- 연맹 버프 CSV 데이터 설정 (기존 빈 값 → 실제 buff_idx/buff_value 채움)
- 새 buff_idx 205 추가 (전체 유닛 공격력, unit/all/attack)
- `_remove_alliance_buff` 버그 수정: target_type 하드코딩 → config 기반 조회 + 연구 버프 삭제 추가
- `_add_alliance_buff` 보완: 가입 시 기존 연구 버프도 함께 적용
- `_get_buff_target_type` 헬퍼 메서드 추가

### 연맹 버프 설계

| 출처 | buff_idx | 효과 | buff_value |
|------|----------|------|------------|
| 연맹 레벨 1~7 | 103 | 건설 시간 감소 | 1~7% |
| 연구 8001 Lv1~5 | 205 (신규) | 전체 공격력 증가 | 1~5% |
| 연구 8002 Lv1~5 | 104 | 연구 시간 감소 | 1~5% |
| 연구 8003 Lv1~5 | 101 | 자원 생산 증가 | 1~5% |

### 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| `meta_data/buff_info.csv` | buff_idx 205 추가 (unit/all/attack) |
| `meta_data/alliance_level.csv` | buff_idx=103, buff_value=1~7 설정 |
| `meta_data/alliance_research.csv` | 8001→205, 8002→104, 8003→101 + buff_value 1~5 |
| `services/game/AllianceManager.py` | `_get_buff_target_type`, `_add_alliance_buff`, `_remove_alliance_buff` 수정 |

---

## [2026-03-09] 연맹 테스트 + 버그 수정

### 작업 내용

- 연맹 시스템 전체 테스트 스위트 신규 작성 (49개 테스트, 17개 API 전수 커버)
- AllianceManager buff 레이어 오류 수정: `BuffRedisManager` → `BuffManager` (4개소)
- buff_info.csv cp949 인코딩 복원
- 공지사항 권한 불일치 수정: 하드코딩 → `_has_permission()` CSV 기반
- 전체 **176 passed**, 0 failed

### 신규 파일

| 파일 | 테스트 수 | 커버리지 |
|------|----------|---------|
| `tests/test_alliance.py` | 49 | API 7001~7017 전수 + 버프 통합(7) + 플로우(2) |

### 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| `services/game/AllianceManager.py` | `_get_buff_manager()` 헬퍼 추가, buff 호출 4개소 수정, 공지 권한 수정 |
| `meta_data/buff_info.csv` | cp949 인코딩 복원 + buff_idx 205 행 유지 |

---

## [2026-03-09] 집결(Rally) 시스템 구현

### 작업 내용

- NPC 집결(Rally) 시스템 신규 구현 (API 9031~9035)
- 2단계 행군: gather(멤버→Leader 성) + attack(Leader 성→NPC)
- recruit_window(1분/5분) 모집 시간제 + 전원 도착 시 자동 발사
- 전투 시 Leader 영웅/연구만 적용, 멤버는 병사만 제공
- 전투 종료 후 유닛 비례 분배 → 멤버별 개별 귀환 march
- BattleWorker에서 rally_npc 전투 알림 대상을 멤버 전원으로 확장

### 신규 파일

| 파일 | 내용 |
|------|------|
| `services/game/RallyManager.py` | rally_create, rally_join, rally_info, rally_kick, rally_cancel, try_launch_rally |

### 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| `services/redis_manager/combat_redis_manager.py` | Rally Redis CRUD ~15개 메서드 추가 (rally metadata, members, recruit queue) |
| `services/game/BattleManager.py` | `rally_npc_battle_start`, `_rally_npc_battle_end`, `_distribute_survived_units` 추가 + battle_type 분기 |
| `services/background_workers/task_worker.py` | rally_gather/rally_attack/rally_recruit 핸들러 3개 추가 |
| `services/background_workers/battle_worker.py` | rally_npc 전투 알림 대상 전체 멤버로 확장 |
| `services/system/APIManager.py` | 9031~9035 라우팅 등록 |
| `services/game/__init__.py` | RallyManager import/export 추가 |

### Redis 키

| 키 | 타입 | 용도 |
|----|------|------|
| `rally:id:counter` | String | rally_id 원자적 생성 |
| `rally:{rally_id}` | String(JSON) | rally 메타데이터 |
| `rally:{rally_id}:members` | Hash | 멤버별 참여 정보 |
| `completion_queue:rally_recruit` | ZSet | 모집 만료 큐 |

---

## v0.6.4 — 성 일반전투 (multi-attacker + 무혈입성)

> 구현일: 2026-03-09

### 기능 요약

동일 수비자를 대상으로 여러 공격자가 동시에 공격할 수 있는 성 전투 시스템. 스냅샷 기반 그룹 틱 처리, 기여도 비율 약탈 분배, 무혈입성(수비 0) 지원.

### 기획 확정 사항

| 항목 | 결정 |
|------|------|
| 약탈 분배 | 기여도(수비에 가한 HP 피해) 비율 |
| 수송 용량 | 무제한 (추후 확장 예정) |
| 라운드 제한 | 없음 |
| 무혈입성 | 수비 병력 0 → 약탈만 진행 |

### 핵심 설계

- **스냅샷 기반**: 동일 수비자 대상 모든 전투가 같은 틱에서 동일한 수비 스냅샷 기준으로 계산
- **수비 반격 DPS**: 기본 DPS × 공격자 수 (각 공격자에게 full DPS 적용)
- **기여도 추적**: `damage_dealt` 필드로 수비에게 준 HP 피해 누적
- **castle_battle Set**: `castle_battle:{defender_no}` 로 동일 수비자 대상 전투 그룹핑

### 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| `services/redis_manager/combat_redis_manager.py` | `castle_battle:{defender_no}` Set CRUD 3개 메서드 추가 |
| `services/game/BattleManager.py` | `battle_start()` 무혈입성/기존전투동기화 추가, `process_castle_tick()` 그룹 틱 신규, `_castle_attacker_end()` `_castle_all_attackers_win()` 신규 |
| `services/background_workers/battle_worker.py` | `_process_pending()` user 전투 defender별 그룹핑, 알림 메서드 분리 |
| `services/background_workers/task_worker.py` | 무혈입성 결과 처리 분기 추가 |

### Redis 키 변경

| 키 | 타입 | 용도 |
|----|------|------|
| `castle_battle:{defender_no}` | Set | 수비자 대상 활성 전투 ID 목록 |

### battle state 필드 추가

| 필드 | 용도 |
|------|------|
| `battle_type: "user"` | 명시적 타입 지정 (기존 default에서 변경) |
| `damage_dealt` | 이 공격자가 수비에게 준 누적 HP 피해 (기여도) |
| `def_units_original` | 전투 시작 시점 수비 병력 (종료 시 ready 차감용) |

### WebSocket 이벤트 추가

| 이벤트 | 발신 대상 | 내용 |
|--------|----------|------|
| `battle_bloodless` | 공격자 | 무혈입성 완료 + 약탈 정보 |
| `battle_bloodless_defend` | 수비자 | 무혈입성 당함 알림 |

### 테스트 (17개)

| 파일 | 테스트 수 | 커버리지 |
|------|----------|---------|
| `tests/test_castle_battle.py` | 17 | battle_start(3), bloodless(2), existing battle sync(2), castle tick single(2), castle tick multi(2), attacker lose(2), def_units_original(2), calculate_round(2) |

- BattleManager 직접 테스트 (Worker-internal 메서드이므로 HTTP API 경유 아님)
- 순환 import 방지: `create_battle_manager()` 내부에서 deferred import
- 전체 **193 passed** (176 기존 + 17 신규)

### API.md WebSocket 이벤트 명세 추가

9개 WebSocket 이벤트 타입 명세 작성: `battle_start`, `battle_tick`, `battle_end`, `battle_incoming`, `battle_bloodless`, `battle_bloodless_defend`, `map_march_update`, `map_march_complete`, `battlefield_tick`

---

## v0.7 — C++ 전투 계산 포팅 (예정)

### 목표

- `BattleManager.calculate_round()` Python → C++ 구현 후 pybind11로 연동
- 포트폴리오용 벤치마크 비교 (Python vs C++ 처리 속도)
- 동일 입출력 인터페이스 유지 (Game Layer 코드 변경 최소화)

### 예상 작업

| 작업 | 내용 |
|------|------|
| `cpp/battle_calc.cpp` | `calculate_round()` C++ 구현 |
| `cpp/CMakeLists.txt` | pybind11 빌드 설정 |
| `BattleManager.py` (수정) | C++ 모듈 import → fallback Python |
| 벤치마크 스크립트 | 1000회 시뮬레이션 속도 비교 |

---

## 미구현 / 취소된 기능

| 기능 | 상태 | 비고 |
|------|------|------|
| C++ 전투 계산 | 예정 (v0.7) | Python 구현 완료, C++ 포팅 대기 |
| 영웅 스킬 시스템 | 미설계 | hero_skill.csv 플레이스홀더만 존재 |
| 루비 상점 | 미구현 | ruby 재화만 존재 |
| 도감(Codex) | 취소 | CodexManager 코드만 잔존 |

---

## [2026-03-06] Unit 미해결 이슈 수정 패치

- `unit_cancel`(4005), `unit_speedup`(4006), `get_completion_status`를 DB `UnitTasks` 의존에서 Redis metadata 기반으로 전환
- `unit_finish`(4004) APIManager 등록 (주석 해제)
- `invalidate_unit_cache` / `get_cache_info`의 `_get_units_meta_key` 오타 수정
- `update_unit_completion_time` 시그니처 수정 (unit_idx → unit_type as task_id)
- 테스트 10개 추가 (finish 3, cancel 4, speedup 3) → 전체 70 passed

**변경 파일**: `UnitManager.py`, `unit_redis_manager.py`, `APIManager.py`, `test_unit.py`, `API.md`, `BUG_LOG.md`, `TEST_LOG.md`

---

## [2026-03-10] 클라이언트 HTML 미호출 API 구현

### 작업 내용

서버에 등록되어 있으나 HTML에서 호출되지 않던 6개 api_code를 클라이언트에 구현.

| 파일 | 추가된 API | 기능 |
|------|-----------|------|
| main.html | 1003 | 신규 유저 생성 버튼 + 자동 ID 입력 |
| main.html | 1009 | 국가 정보 조회 → 헤더에 닉네임/레벨/전투력/좌표 표시 |
| buff.html | 1012 | 버프 상세 조회 (총합/상세 모드 전환) |
| buff.html | 1014 | 타입별 버프 필터 (unit/resource/building/research) |
| item.html | 6002 | 테스트 아이템 획득 폼 (item_idx + 수량 입력) |
| battle.html | 9001 | 내 맵 좌표 조회 버튼 |

### 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| `templates/main.html` | 헤더에 "신규 유저 생성" 버튼 + nationInfo 표시 추가, `createNewUser()`, `loadNationInfo()` 함수 |
| `templates/buff.html` | 총합/상세 모드 토글, 타입 필터 select, `loadBuffDetail()`, `loadBuffByType()`, `renderDetailBuffs()`, `renderTypeFilteredBuffs()` |
| `templates/item.html` | "테스트 아이템 획득" 폼, `getItem()` 함수 |
| `templates/battle.html` | "내 좌표 조회" 버튼, `loadMyPosition()` 함수 |

---

## [2026-03-10] DB 자동 테이블 생성 + 유저 위치 Redis-only 전환 + CSV/클라이언트 버그 수정

### 작업 내용

**DB 테이블 자동 생성**
- 서버 시작 시 `models.Base.metadata.create_all(bind=engine)` 호출 추가
- models.py에 정의된 테이블이 DB에 없으면 자동 생성 (기존 테이블은 변경하지 않음)
- 신규 유저 생성 시 `battle` 테이블 미존재 에러 해결

**stat_nation에서 map_x/map_y 제거 → Redis-only 전환**
- 유저 전장 위치는 일시적 상태 (전장 자유 참가/이탈)이므로 DB 영속화 불필요
- `models.py`: StatNation에서 `map_x`, `map_y` 필드 제거
- `nation_db_manager.py`: `_serialize_model()`에서 map_x/map_y 제거
- `MapManager.py`: DB 폴백 조회 제거, DB 저장 제거 → Redis-only
- `MarchManager.py`: `_get_position()` DB 폴백 제거 → Redis-only
- MySQL: `ALTER TABLE stat_nation DROP COLUMN map_x, DROP COLUMN map_y` 실행

**unit_info.csv 한글 인코딩 복구**
- 한글 데이터가 U+FFFD(대체 문자)로 손상되어 있었음
- UTF-8로 올바른 한글명 복원 (보병, 중보병, 검사, 팔라딘, 기병, 중기병, 기사, 왕실기병대, 궁병, 척후병, 저격수, 호크아이)

**unit.html 능력치 차트 수정**
- `createUnitStatsChart()` 함수의 랜덤 데이터(`Math.random()`)를 실제 CSV 능력치(`unit.ability`)로 교체

### 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| `main.py` | startup_event에 `models.Base.metadata.create_all()` 추가 |
| `models.py` | StatNation에서 `map_x`, `map_y` 필드 제거 |
| `services/db_manager/nation_db_manager.py` | `_serialize_model()`에서 map_x/map_y 제거 |
| `services/game/MapManager.py` | DB 폴백/저장 제거 → Redis-only 위치 관리 |
| `services/game/MarchManager.py` | `_get_position()` DB 폴백 제거 → Redis-only |
| `meta_data/unit_info.csv` | 한글명 인코딩 복구 (UTF-8) |
| `templates/unit.html` | 능력치 차트 랜덤→실데이터 |

---

## [2026-03-12] 전투 시스템 테스트 Phase 2~5 구현

### 작업 내용

전투 시스템 단위 테스트 Phase 2~5 구현 완료. Phase 1(NPC 전투 21개)은 이전 세션에서 완료.

| 파일 | 테스트 수 | 커버리지 |
|------|----------|---------|
| `tests/test_npc_battle.py` | 21 | Phase 1: npc_battle_start(4), process_battle_tick(8), _npc_battle_end(9) |
| `tests/test_battle_engine.py` | 28 | Phase 2: _calc_army_stats(5), calculate_round(5), _hero_coefficients(6), _get_hero_skill(5), _check_rage_skill(7) |
| `tests/test_rally_npc_battle.py` | 16 | Phase 3: rally_npc_battle_start(4), _rally_npc_battle_end(5), _distribute_survived_units(7) |
| `tests/test_battle_api.py` | 17 | Phase 4+5: battle_info API(5), battle_report API(3), 엣지케이스(2), 동시성(3), 정합성(4) |

- 전투 테스트 합계: **96개** (기존 castle 14개 포함)
- 전체 테스트 스위트: **275 passed**, 0 failures

### 신규 파일

| 파일 | 내용 |
|------|------|
| `tests/test_battle_engine.py` | 전투 계산 엔진 순수 로직 테스트 (DB/Redis 없이) |
| `tests/test_rally_npc_battle.py` | Rally NPC 전투 시작/종료/유닛 분배 테스트 |
| `tests/test_battle_api.py` | 전투 API + 엣지케이스 + 동시성 + 정합성 테스트 |

### 테스트 기법

- 순환 import 방지: 함수 레벨 lazy import (`def _bm(): from ... import BattleManager`)
- DB 없이 인스턴스 생성: `BM.__new__(BM)` 패턴 (_calc_army_stats 등 instance method 테스트용)
- Redis "None" 문자열 엣지케이스: hero_idx="None" (Redis 직렬화) 처리 검증

---

## [2026-03-11] 유닛 훈련/업그레이드 완료 처리 버그 수정 (4건)

### 작업 내용

유닛 업그레이드 완료가 영구적으로 처리되지 않는 복합 버그 4건 수정. 플레이어 8의 유닛 401이 `upgrading=10` 상태에서 영구 고착된 현상의 근본 원인 분석 및 해결.

### 수정된 버그

| # | 위치 | 원인 | 해결 |
|---|------|------|------|
| 1 | `base_redis_task_manager.py` | 메타데이터 TTL(86400s) 만료 시 Sorted Set 멤버는 남아있어, Worker가 빈 metadata로 처리 → task_type=-1 → 큐에서만 제거하고 유닛 상태 미변경 | 메타데이터 TTL 제거 (remove_from_queue에서 함께 삭제) |
| 2 | `unit_redis_manager.py` register_active_tasks_to_queue | 로그인 시 `training > 0`만 체크, `upgrading > 0` 유닛은 큐 재등록 안 됨 | upgrading 유닛도 큐 재등록 또는 강제 완료 처리 추가 |
| 3 | `UnitManager.py` recover_orphaned_tasks | 고아 복구에서 `training > 0`만 체크, `upgrading > 0` 무시 | upgrading 유닛도 ready로 강제 복구 처리 추가 |
| 4 | `UnitManager.py` _handle_unit_upgrade | 업그레이드 완료 시 target 유닛의 `ready` 미증가 (total만 올림) | `target_unit_data['ready']` 증가 추가 |

### 추가 개선

- `unit_upgrade()`: 캐시에 `training_end_time`, `upgrade_target_unit_idx` 저장 (로그인 복구 시 큐 재등록 가능하도록)
- `_handle_unit_upgrade()`: 완료 시 `training_end_time`, `upgrade_target_unit_idx` 필드 정리

### 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| `services/redis_manager/base_redis_task_manager.py` | 메타데이터 TTL(86400s) 제거 |
| `services/redis_manager/unit_redis_manager.py` | `register_active_tasks_to_queue()` upgrading 처리 추가 |
| `services/game/UnitManager.py` | `unit_upgrade()` 캐시 필드 추가, `_handle_unit_upgrade()` target ready 증가 + 필드 정리, `recover_orphaned_tasks()` upgrading 처리 추가 |

### 테스트

- `python -m pytest tests/test_unit.py -v` — 28 passed

---

## infra — Claude Code 구조 재편 (2026-03-12)

### 작업 내용
Claude Code 세션 연속성·컨텍스트 효율·워크플로우 강제성 문제 해결을 위한 구조 재편.

### 변경 파일
- `CLAUDE.md` — 547줄 → 173줄 경량화 (Step 0~5 상세 및 부가 섹션 rules/로 이동)
- `.claude/rules/workflow.md` — 신규. Step 0~5 전체 상세 (항상 로드)
- `.claude/rules/new-api.md` — 신규. api_manager.py path 스코프
- `.claude/rules/db.md` — 신규. models.py, db_manager/** path 스코프
- `.claude/rules/redis.md` — 신규. redis_manager/**, background_workers/** path 스코프
- `.claude/rules/combat.md` — 신규. Battle/March/MapManager.py path 스코프
- `.claude/rules/testing.md` — 신규. tests/** path 스코프
- `.claude/hooks/pre_write_check.py` — 신규. services/*.py 수정 전 Step 1 소프트 체크
- `.claude/hooks/post_test_remind.py` — 신규. tests/*.py 수정 후 pytest 리마인더
- `.claude/settings.json` — hooks PreToolUse/PostToolUse 등록
- `memory/MEMORY.md` 외 3개 — 신규. 세션 연속성용 memory 파일

---

## [2026-03-13] Locust 부하 테스트 프레임워크 구축

### 작업 내용

전투 시스템 중심의 부하 테스트/스트레스 테스트 인프라 구축. Locust 기반 5-Phase 시나리오 + Claude 에이전트 룰.

### 신규 파일

| 파일 | 내용 |
|------|------|
| `load_tests/config.py` | 공통 설정 (서버 URL, API 코드, 프로파일 5종) |
| `load_tests/helpers.py` | API 호출 래퍼, 유저 셋업 믹스인 |
| `load_tests/run.py` | 실행 런처 (Phase 선택 + 프로파일 + headless/web) |
| `load_tests/phase1_api_throughput.py` | Phase 1: 읽기 전용 API 처리량 + Redis 풀 모니터링 |
| `load_tests/phase2_march_battle.py` | Phase 2: NPC 행군/전투 생성 + Worker 부하 |
| `load_tests/phase3_castle_siege.py` | Phase 3: N명 동시 성 공격 (PvP 경합) |
| `load_tests/phase4_battlefield_ws.py` | Phase 4: 전장 + WebSocket 브로드캐스트 스트레스 |
| `load_tests/phase5_combined.py` | Phase 5: 복합 시나리오 (50% 조회 + 25% PvE + 15% PvP + 10% 전장) |
| `.claude/agents/load-test.md` | Claude 에이전트 룰 (실행/분석/병목 진단 자동화) |

### 테스트 시나리오 설계

| Phase | 시나리오 | 핵심 검증 |
|-------|---------|----------|
| 1 | 읽기 전용 API 반복 호출 | 기본 RPS, p95 응답시간 |
| 2 | NPC 행군 + 유닛 훈련 병행 | TaskWorker/BattleWorker 동시 처리 |
| 3 | N명 → 1명 성 동시 공격 | castle_tick 그룹 스냅샷 정합성, 약탈 분배 |
| 4 | 전장 참여 + WS 구독 + 전투 | WS 브로드캐스트 부하, 메시지 수신 지연 |
| 5 | 전체 혼합 부하 | Redis 풀 고갈 시점 (max 50), 서버 한계 탐색 |

### 프로파일

| 이름 | 유저 수 | 시간 | 용도 |
|------|---------|------|------|
| smoke | 5 | 30s | 기본 동작 확인 |
| light | 20 | 2m | 경량 부하 |
| medium | 50 | 5m | Redis 풀 한계 근접 |
| heavy | 100 | 10m | Redis 풀 초과 |
| stress | 200 | 15m | 서버 한계 탐색 |

---

## 알려진 기술 부채

| 항목 | 설명 | 우선순위 |
|------|------|---------|
| DB DDL 미실행 | March/Battle/Hero 테이블, StatNation map_x/map_y는 models.py에만 반영됨. 실제 DB에 ALTER/CREATE 필요 | 높음 |
| 방어자 유닛 DB 폴백 미구현 | `battle_start()` 시 Redis 캐시 없으면 def_units 빈 dict로 처리 | 중간 |
| hero_skill ~~미적용~~ | hero_skill.csv 기반 rage 스킬 시스템 구현 완료 (전투 테스트 28개 검증) | ✅ 해결 |
| NPC 서버 재시작 시 초기화 | Redis map:npcs는 메모리이므로 서버 재시작 시 NPC 위치 재배치됨 | 낮음 |
